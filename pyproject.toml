[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "berkshire-nexus"
version = "1.3.0"
description = "Current market data, cited news, configurable AI research, adaptive learning, and risk-gated Binance Stocks integration"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "OpenSource Contributor" }
]
dependencies = []

[project.scripts]
berkshire-nexus = "src.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
