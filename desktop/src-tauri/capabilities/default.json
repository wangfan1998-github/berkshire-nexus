{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the BerkshireNexus main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
